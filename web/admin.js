const API='https://my-level-api.onrender.com';const root=document.querySelector('#devices');const count=document.querySelector('#count'),online=document.querySelector('#online'),sharing=document.querySelector('#sharing');function esc(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function render(ds){count.textContent=ds.length;online.textContent=ds.filter(d=>d.status==='online').length;sharing.textContent=ds.filter(d=>d.sharing).length;if(!ds.length){root.innerHTML='<div class="empty"><strong>No computers connected</strong><span>Authorized computers will appear here after pairing.</span></div>';return}root.innerHTML=ds.map(d=>`<article class="device"><div class="device-main"><div class="device-icon">▣</div><div class="device-copy"><h3>${esc(d.name)}</h3><div class="meta"><span>${esc(d.platform)}</span><span class="meta-dot"></span><span>${d.sharing?'Screen sharing on':'Screen sharing off'}</span></div></div></div><div class="device-side"><span class="status ${d.status}">${d.status}</span><button class="view-button" ${d.status!=='online'||!d.sharing?'disabled':''}>View screen</button></div></article>`).join('')}async function load(){const r=await fetch(API+'/api/admin/devices',{credentials:'include',cache:'no-store'});if(r.status===401){location.href='/admin-login.html';return}render(await r.json())}document.querySelector('#refresh').onclick=load;document.querySelector('#logout').onclick=async()=>{await fetch(API+'/api/admin/logout',{method:'POST',credentials:'include'});location.href='/'};load();